## For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
#
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Default value: -Xmx1024m -XX:MaxPermSize=256m
# org.gradle.jvmargs=-Xmx2048m -XX:MaxPermSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
#
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. For more details, visit
# https://developer.android.com/r/tools/gradle-multi-project-decoupled-projects
# org.gradle.parallel=true
#Tue Dec 16 17:35:42 WAT 2025
android.enableJetifier=true
android.useAndroidX=true
# Increase Gradle Daemon heap size to 4GB
org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8

# Optional: Enable parallel execution and caching for faster builds
org.gradle.parallel=true
org.gradle.caching=true

# KMP: iOS targets are declared in :shared. On Linux CI they are disabled with a
# warning; this silences that warning (Android and host tests do not need them).
kotlin.native.ignoreDisabledTargets=true
